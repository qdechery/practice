<!doctype html>
<html>
	<head>
		<title><%= title %></title>
	</head>

	<body>
		<% if ( userFullName ) { %>
			<h2>Hello <%=userFullName%> </h2>
			<a href="/signout">Sign out</a>
		<% } else { %>
			<a href="/signup">Sign up</a>
			<a href="/signin">Sign in</a>
			<a href="/signout">Sign out</a>
		<% } %>

		<br />
		<img src="img/logo.jpg" width="500px" height="500px" alt="logo">
		<h1><%= title %></h1>
	</body>
</html>